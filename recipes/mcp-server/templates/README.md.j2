# {{ project_name }}

Generated by Metis as a minimal Python MCP server using the **{{ transport }}** transport.

## Example tool

The server exposes one safe starter tool, `echo`, which returns the message it receives.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

{% if transport == "streamable-http" %}Start the server:

```bash
python -m app.server
```

The MCP endpoint is available at `http://localhost:8000/mcp`.
{% else %}Start the server over stdio:

```bash
python -m app.server
```

Configure your MCP client to launch `python -m app.server` from this project directory and connect using stdio.
{% endif %}

## Docker Compose

```bash
docker compose up --build
```

## Test

```bash
pytest
```

Extend the server by adding narrowly scoped tools to `app/tools.py` and registering them from `app/server.py`.